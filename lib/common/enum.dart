enum CustomProfileType {
  enable,
  disable,
  global,
}

enum DownloadOrigImageType {
  always,
  askMe,
  no,
}

enum TabletLayout {
  automatic,
  // always,
  landscape,
  never,
}

enum AvatarBorderRadiusType {
  circle,
  roundedRect,
}

enum AvatarType {
  boringAvatar,
  textAvatar,
}

enum TextAvatarsType {
  noText,
  firstText,
  firstTowText,
  onlyBorder,
  borderFirstText,
  borderFirstTowText,
  transparent,
}
