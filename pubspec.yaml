name: fehviewer
description: ..

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.4+149

environment:
  sdk: '>=2.10.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  # 国际化
  flutter_localizations:
    sdk: flutter

  intl: ^0.17.0
#  intl: ^0.16.1

  # 图标
  cupertino_icons: ^1.0.0
  font_awesome_flutter: ^8.11.0
  line_icons: ^1.3.2

  # 网络请求
  dio: ^3.0.10
  dio_http_cache: ^0.2.11

  # cookie
  cookie_jar: ^1.0.1
  dio_cookie_manager: ^1.0.0
  webview_cookie_manager: ^1.0.5

  #shared_preferences插件 持久化
  shared_preferences: '>=0.5.7+3'
#  shared_preferences:
#    git:
#      url: https://github.com/flutter/plugins.git
#      path: packages/shared_preferences/shared_preferences

  # toast
  oktoast: ^2.3.2

  # 下拉刷新
  # flutter_easyrefresh: ^2.1.1

  # 数据库
  sqflite: ^1.3.2+3

  # html反转义
  html_unescape: ^1.0.2

  enum_to_string: ^1.0.14

  # webview
  flutter_webview_plugin: ^0.3.11
#  webview_flutter:
#    git:
#      url: https://github.com/flutter/plugins.git
#      path: packages/webview_flutter
  webview_flutter: ^1.0.7
  flutter_inappwebview: ^4.0.0+4
#  flutter_inappwebview:
#    path: /Users/honjow/git/flutter_inappwebview

  html: ^0.14.0+4

  # 文字链接
  flutter_linkify: ^4.0.2
  url_launcher: ^5.7.10

  # 元组
  tuple: ^1.0.3

  # 状态管理
#  provider: ^4.3.2+2

# 目前的3.25.x版本存在refresh的bug。github版本已修复 暂时使用GitHub源
#  get: 3.25.4
  get_storage: ^1.4.0

  photo_view: ^0.10.3

  # 图片相关
  cached_network_image: ^2.5.0
  extended_image: ^2.0.0

  liquid_progress_indicator: ^0.3.2

  logger: ^0.9.4
#  logger_flutter:
#    git:
#      url: https://github.com/ewertonrp/logger_flutter.git

  # 瀑布流
  waterfall_flow: any

  modal_bottom_sheet: ^1.0.0+1

  # 生物认证 指纹 面容等
  local_auth: ^0.6.3+4

  # 彩蛋
  flutter_egg: ^1.0.3

  # 从分享菜单打开
  receive_sharing_intent: ^1.4.3

  #device_preview: ^0.5.5

  # 分享
  share: ^0.6.5+4
#  share:
#    git:
#      url: https://github.com/flutter/plugins.git
#      path: packages/share

#  sensors:
#    git:
#      url: https://github.com/flutter/plugins.git
#      path: packages/sensors

  # 权限检查
  permission_handler: ^5.1.0+2
#  permission_handler:
#    git:
#      url: https://github.com/Baseflow/flutter-permission-handler.git
#      path: permission_handler

  flutter_cache_manager: ^2.1.1

  # 图片保存
  image_gallery_saver: ^1.6.8

  popup_menu: ^1.0.5

  # 滑动删除
  flutter_slidable: ^0.5.7

  scrollable_positioned_list: ^0.1.9

  reorderables: ^0.3.2

  dns_client: ^0.1.0+2

  # 震动
  flutter_vibrate: ^1.0.0

  # 文件选择
  file_picker: ^2.1.5+1

#  flutter_screenutil:
#    git:
#      url: git://github.com/OpenFlutter/flutter_screenutil.git

  like_button:
    git:
      url: git://github.com/fluttercandies/like_button.git

  flutter_rating_bar: ^3.2.0+1

  custom_pop_up_menu: ^1.1.0

  # flutter_downloader: ^1.5.2

  # 自定义进度步进
#  flutter_downloader:
#    git:
#      url: https://github.com/nimisis/flutter_downloader.git
  flutter_downloader:
    path: /Users/honjow/git/flutter_downloader_honjow

  floor: ^0.18.0
  copy_with_extension: ^1.4.0
  package_info: ^0.4.3+4

  firebase_analytics: any
  firebase_crashlytics: any
  firebase_performance: any
  dio_firebase_performance: ^0.3.0
  pretty_dio_logger: ^1.1.1
  dio_retry: ^0.1.9-beta
  translator: ^0.1.5
#  flutter_screen_lock: ^1.2.6
  pattern_lock: ^1.0.3
  orientation: ^1.2.0
  vibration: ^1.7.3



dev_dependencies:
#  build_runner: ^1.0.0
#  json_serializable: ^2.0.0
#  # flutter packages pub run json_model src=json_files  dist=data # will save in lib/data dir
#  json_model: ^0.0.2
  json_to_model: #flutter pub run json_to_model   ### json_model太久不更新了 依赖版本太低
    git:
      url: git://github.com/fadhilx/json_to_model.git
#  json_to_model:
#    path: /Users/honjow/git/json_to_model
  build_runner: ^1.10.3
#  build_runner:
#    path: /Users/honjow/git/build/build_runner
  json_serializable: ^3.3.0
  json_annotation: ^3.0.1
#
  intl_translation: ^0.17.10+1
  floor_generator: ^0.18.0
  copy_with_extension_gen: ^1.4.0

  flutter_test:
    sdk: flutter

dependency_overrides:
  build_runner: ^1.11.5
  analyzer: ^1.1.0
  args: ^2.0.0
  crypto: ^3.0.0
  yaml: ^3.0.0
  watcher: ^1.0.0
  package_config: ^2.0.0
  glob: ^2.0.0
  convert: ^3.0.0
  build: ^1.6.3
  get:
    git: git://github.com/jonataslaw/getx.git
  intl: ^0.17.0

flutter_intl:
  enabled: true

flutter:
  uses-material-design: true
  # fonts:
  #   - family: SourceHanSansSC
  #     fonts:
  #       - asset: fonts/SourceHanSansSC-Regular.otf
